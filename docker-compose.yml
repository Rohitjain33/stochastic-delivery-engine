version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stochastic-delivery-simulator
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - DEBUG=True
